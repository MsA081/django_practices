[project]
name = "django-practices"
version = "0.1.0"
description = "a test project"
authors = [
    {name = "mohammad aminifar"}
]
license = {text = "none"}
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django (>=6.0.2,<7.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
